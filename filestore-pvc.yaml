apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: filestore-prod-network-sc
provisioner: filestore.csi.storage.gke.io
volumeBindingMode: Immediate
allowVolumeExpansion: true
parameters:
  tier: BASIC_HDD
  network: jenkins-prod-vpc
