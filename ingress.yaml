apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-ingress
  annotations:
    kubernetes.io/ingress.class: "gce"
spec:
  defaultBackend:
    service:
      name: ui-service
      port:
        number: 80
  rules:
  - http:
      paths:
      # 1. Route the admin page explicitly to your UI container
      - path: /admin.html
        pathType: Exact
        backend:
          service:
            name: ui-service
            port:
              number: 80

      # 2. Route standard traffic to index.html
      - path: /*
        pathType: ImplementationSpecific
        backend:
          service:
            name: ui-service
            port:
              number: 80
      
      - path: /api/products/*
        pathType: ImplementationSpecific
        backend:
          service:
            name: api-3-service
            port:
              number: 80

      - path: /api/write/*
        pathType: ImplementationSpecific
        backend:
          service:
            name: api-2-service
            port:
              number: 80

      - path: /api/read/*
        pathType: ImplementationSpecific
        backend:
          service:
            name: api-1-service
            port:
              number: 80
